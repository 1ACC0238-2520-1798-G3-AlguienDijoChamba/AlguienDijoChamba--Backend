namespace AlguienDijoChamba.Api.Customers.Domain.Enum;

// Enum para representar los métodos de pago disponibles
public enum PreferredPaymentMethod
{
    None = 0, // Valor por defecto
    CreditDebitCard = 1,
    DigitalWallet = 2
}